var a = 10;
const b = "hello";
let c = true;

let array = [1, 2, 3];

for (let i = 0; i < array.length; i++) {
    const element = array[i];
    document.write(element);
}

if(c) {
    document.write(`${a} ${b}`);
}

